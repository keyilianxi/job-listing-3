<% if flash.any? %>
  <% user_facing flashes.each do |key, velue| %>
    <div class="alert alert-dismissable alert-<%= flash_class(key)%>">
      <button class="close" data-dismiss="alert">x</button>
      <%= value %>
    </div>
  <% end %>
<% end %>
